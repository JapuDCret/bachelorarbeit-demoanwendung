import { SplunkLoggingMonitor } from './splunk-logging-monitor';

describe('SplunkLoggingMonitor', () => {
  it('should create an instance', () => {
    expect(new SplunkLoggingMonitor()).toBeTruthy();
  });
});
