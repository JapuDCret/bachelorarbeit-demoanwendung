import { SplunkForwardingErrorHandler } from './splunk-forwarding-error-handler';

describe('SplunkForwardingErrorHandler', () => {
  it('should create an instance', () => {
    expect(new SplunkForwardingErrorHandler()).toBeTruthy();
  });
});
